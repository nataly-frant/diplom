# Information and reference system

## Project init

- Init the migration in docker `docker compose exec backend alembic upgrade head`
- For population the DB run the script `populate_db.py` from `backend/scripts` locally
